// Test module access syntax

use simple_math;

fn main() -> i32 {
    let result = simple_math::add(10, 20)
    let product = simple_math::multiply(5, 6)
    
    println("Result: {result}, Product: {product}")
    return 0
}
