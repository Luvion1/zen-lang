// Basic ownership transfer example
fn main() -> i32 {
    let x = 42
    let y = <-x  // Move x to y
    println("Value: {y}")
    // println(x)  // This would cause an error: use of moved variable
    return 0
}
